# Core backend — install this first: pip install -r requirements.txt
Django>=5.0,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
django-filter>=24.2
celery>=5.4
redis>=5.0
Pillow>=10.4
qrcode>=7.4
reportlab>=4.2
python-dotenv>=1.0
requests>=2.32
whitenoise>=6.7
gunicorn>=22.0
dj-database-url>=2.2

# Postgres driver — only needed when POSTGRES_DB is set (docker-compose /
# production). Local dev falls back to sqlite without it.
psycopg2-binary>=2.9
