# Base URL of the Django API. Leave unset in dev — the Vite dev server
# proxies /api to http://localhost:8000 (see vite.config.js). Set this
# only for a build that talks to a different host than the one it's
# served from.
# VITE_API_BASE_URL=https://api.zhasyldala.kz
